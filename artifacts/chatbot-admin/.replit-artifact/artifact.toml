kind = "web"
previewPath = "/"
title = "Chatbot Admin Platform"
version = "1.0.0"
id = "artifacts/chatbot-admin"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21662

[services.development]
run = "pnpm --filter @workspace/chatbot-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chatbot-admin", "run", "build" ]
serve = "static"
publicDir = "artifacts/chatbot-admin/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21662"
BASE_PATH = "/"
